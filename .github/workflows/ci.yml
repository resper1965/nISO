name: CI

on:
  push:
    branches: [main]
  # Sem filtro de branch: `branches: [main]` casa com a BASE do PR, então PR
  # empilhado sobre outro branch não rodava job nenhum — e "0 checks" se parece
  # com "ainda não começou", não com "não vai rodar". Aconteceu no PR #14.
  pull_request:

# O CI só lê o repositório. Sem isso, o GITHUB_TOKEN vem com escrita por padrão.
permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: TypeScript Check
        run: npx tsc --noEmit
      - name: Build Frontend
        run: cd frontend && npm ci && npm run build
      - name: Build MCP Server
        run: cd mcp-server-niso && npm ci && npm run build
      # Retry por flakiness conhecida do @cloudflare/vitest-pool-workers: às vezes
      # o pool emite `fetch failed / other side closed` no teardown entre arquivos
      # (erro do runtime, não do app). Uma falha DETERMINÍSTICA falha nas duas
      # tentativas — o retry só resgata a flakiness, não mascara bug real.
      - name: Run Tests
        run: |
          n=0; max=2
          until npm test -- --run; do
            n=$((n+1))
            if [ "$n" -ge "$max" ]; then echo "::error::Testes falharam após $max tentativas"; exit 1; fi
            echo "::warning::Testes falharam (tentativa $n/$max) — provável flakiness do vitest-pool-workers no teardown; repetindo."
          done
